`<foreignObject width="230" height="17" transform="translate (1060 -164)" style="
    display: block;
    "><overflow-clip style="
        padding: 0;
        background: transparent;
        width: 100%;
        height: 100%;
        display: block;
        overflow: hidden;
        z-index: 9999;
    "
        ><input style="
            outline: none;
            border: none;
            padding: 0px;
            margin: 0px;
            display: block;
            background: transparent;
            width: 1e+07vw;
            height: 100%;
            -webkit-appearance: none;"
    ></overflow-clip
></foreignObject>`


`
input .addEventListener ('focus', function (e) {
	var unblurred = true;
	var one_blur = function () {
		unblurred = false;
		x .removeEventListener ('blur', one_blur, true)
	}
	x .addEventListener ('blur', one_blur, true)

    var refresh = function (){
        x .style .display = (x .style .display === 'block' ? 'flex' : 'block')
        if (document .activeElement === x && unblurred) {
            requestAnimationFrame (q);
    }};
    requestAnimationFrame (refresh);
}, true)
`